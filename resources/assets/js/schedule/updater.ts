import {ScheduledCourse} from "../data/interfaces";
export function sendScheduleUpdates(newCourses: ScheduledCourse[], deletedCourses: ScheduledCourse[],
                                    changes: ScheduledCourse[], onComplete: () => void) {

}